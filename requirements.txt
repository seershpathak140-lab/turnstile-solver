# playwright pinned to 1.54.0 — newer Node drivers (1.55+) crash on
# `pageError.location.url` for real Cloudflare-protected pages. The
# pageError dispatcher in 1.54.0 still guards that field correctly.
playwright==1.54.0
camoufox[geoip]>=0.4
aiohttp>=3.10
